#!/usr/bin/env node // De esa manera, le estamos diciendo a los sistemas *nix que el intérprete de nuestro archivo JavaScript debe /usr/bin/env nodebuscar el nodeejecutable instalado localmente.

const {mdlinks} = require('./mdlinks.js');

